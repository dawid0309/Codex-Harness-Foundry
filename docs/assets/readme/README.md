# README Assets

This directory stores assets used directly by the repository landing page.

- `codex-harness-foundry-demo.gif` is the terminal demo shown in the main `README.md`.
- Regenerate it with `python scripts/generate-readme-demo.py`.
